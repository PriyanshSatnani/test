import Login from '../screens/Login';
import React from 'react';

// This file is the official entry point for the path `/`
export default function RootIndex() {
  // It simply renders the full Login component located in the screens folder.
  return <Login />;
}